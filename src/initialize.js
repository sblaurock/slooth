require(['audio', 'data', 'host', 'session', 'subscriber'], function(Audio, Data, Host, Session, Subscriber) {
  window.Slooth = {
    Audio: Audio,
    Data: Data,
    Host: Host,
    Session: Session,
    Subscriber: Subscriber
  };
});